// Shared motion vocabulary — one easing for reveals, one spring for layout
// morphs, used everywhere so the whole site feels like a single system.
export const EASE = [0.16, 1, 0.3, 1];

export const MORPH = { type: 'spring', stiffness: 300, damping: 33, mass: 0.9 };
